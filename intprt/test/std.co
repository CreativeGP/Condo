###
Condo Standard

Creative GP
01 Jan 2019
###

(if (|x y| x is 3 & y is 4) (

(
    type Bool Things
    
    &: Bool-Bool-Bool = (|x y| bool x * y)
    |: Bool-Bool-Bool = (|x y| bool x + y)
    ¬, not: Bool-Bool-Bool = (|x| bool 1 - x)
    xor: Bool-Bool-Bool = (|x y| if (x is 1 & y is 1) (bool 0) (x | y))    
    if: (_-Bool)-a = (|f| 
)
